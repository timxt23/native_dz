import { Text, View } from 'react-native';
import { Colors } from '../shared/tokens';

export default function Cart() {
	return (
		<View>
			<Text style={{ color: Colors.white }}>Корзина</Text>
		</View>
	);
}
