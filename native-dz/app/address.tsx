import { Text, View } from 'react-native';
import { Colors } from '../shared/tokens';

export default function Address() {
	return (
		<View>
			<Text style={{ color: Colors.white }}>Адреса</Text>
		</View>
	);
}
